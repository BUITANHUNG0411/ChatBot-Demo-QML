# Dev Journal & Current Context

## [May 2026] Current Vibe
- Just transitioned the main navigation from text-buttons to an iconic sidebar layout.
- Implemented a glassmorphism widget in `Weather.qml`.
- Added `Home.qml` grid layout.

## Pending Ideas / Next Vibe
- Need to hook up `weatherControl` C++ backend to fetch real API data.
- Need to implement SVG icons for the sidebar instead of standard text/emojis.
- Connect the Chatbot UI to a local AI API endpoint.

## Known Issues
- `Note.qml` transitions (`add`, `remove`, `displaced`) need optimization for smoother 60fps rendering.